[project]
name = "space-snake"
version = "0.1.0"
description = "3D Snake RL environment powered by JAX with pygfx rendering"
requires-python = ">=3.12"
dependencies = [
    "jax",
    "jaxlib",
    "gymnasium",
    "pygfx",
    "pyyaml",
    "click",
    "glfw>=2.10.0",
]
